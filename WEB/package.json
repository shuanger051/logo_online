{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "turbo run serve --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "1.10.0"
  },
  "author": "",
  "license": "ISC"
}
